<!DOCTYPE html>
<html>
  <head>
    <title>EventManagement</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://use.fontawesome.com/releases/v5.0.6/css/all.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/earlyaccess/notosansjapanese.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Lato:400,900" rel="stylesheet">
  </head>

  <body>

    <header>
      <nav class="navbar navbar-default">
        <div class="container-fluid">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#gnavi">
              <span class="sr-only">EVENTS</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/"><i class="fas fa-glass-martini"></i>EVENTS<i class="fas fa-glass-martini"></i></a>
          </div>
          <div id="gnavi" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
              <% if logged_in? %>
              <li><i class="fas fa-clipboard"></i><a href="/events#index">開催イベント一覧</a></li>
              <li><i class="fas fa-user"></i><%= link_to "マイページ", user_path(current_user.id) %></a></li>
              <li><i class="fas fa-sign-out-alt"></i><%= link_to "ログアウト", session_path(current_user.id), method: :delete %></li>
              <% else %>
              <li><i class="fas fa-sign-in-alt"></i><%= link_to "ログイン", new_session_path %></li>
              <li><i class="fas fa-user-plus"></i><%= link_to "新規登録", new_user_path %></li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>

    <div class="flash_msg">
      <% flash.each do |key, value| %>
        <%= content_tag(:div, value, class: "#{key}") %>
      <% end %>
    </div>

    <%= yield %>

    <footer>
      <p>DIVE INTO VODE 2017年10月期</p>
      <p>並木　一樹</p>
    </footer>

  </body>
</html>
