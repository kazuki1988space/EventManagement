<div class="mainContainer">
  <div class="pageTitle logIn">ログイン</div>
  <div class="container logIn">

    <div class="form submit logIn">
    <%= form_with(scope: :session, url: sessions_path) do |f| %>
      <table>
          <tr>
            <th><%= f.label "メールアドレス" %></th>
            <td><%= f.email_field :email, class:'email', placeholder: '255字以内' %></td>
          </tr>
          <tr>
            <th><%= f.label "パスワード" %></th>
            <td><%= f.password_field :password, class:'password', placeholder: '6〜20字' %></td>
          </tr>
      </table>
      <div class="page-link toUserShow">
        <%= f.submit "ログイン", class: 'btn btn-warning' %>
      </div>
    <% end %>
    </div>

  </div>
</div>
